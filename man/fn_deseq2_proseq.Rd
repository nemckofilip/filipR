% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_deseq2_proseq.R
\name{fn_deseq2_proseq}
\alias{fn_deseq2_proseq}
\title{DESeq2 Differential Expression for PRO-seq}
\usage{
fn_deseq2_proseq(
  umi.count.tables,
  sample.names,
  conditions,
  ref.genome.stat.files,
  ctl.conditions = unique(conditions),
  normalization = "libSize",
  output.prefix,
  feature,
  dds.output.dir = "db/dds/",
  FC.tables.output.dir = "db/FC_tables/",
  MAplots.output.dir = "pdf/MAplots/"
)
}
\arguments{
\item{umi.count.tables}{Character vector of count tables (\code{.txt}) from \code{\link[=fn_count_proseq_reads]{fn_count_proseq_reads()}}.}

\item{sample.names}{Character vector of sample names (one per count table).}

\item{conditions}{Character vector of conditions (one per count table).}

\item{ref.genome.stat.files}{Character vector of \verb{_UMI_stats.txt} files (one per count table).}

\item{ctl.conditions}{Character vector of control conditions. Default: all unique conditions.}

\item{normalization}{"libSize" (default) or "default".}

\item{output.prefix}{Prefix for output files.}

\item{feature}{Feature label (e.g. "genes").}

\item{dds.output.dir}{Directory for the DESeq2 object. Default 'db/dds/'.}

\item{FC.tables.output.dir}{Directory for the fold-change table. Default 'db/FC_tables/'.}

\item{MAplots.output.dir}{Directory for the MA plots. Default 'pdf/MAplots/'.}
}
\value{
data.table with columns: dds, FC_table, MA_plot, cmd, path.
}
\description{
Builds a shell command to run DESeq2 differential expression on PRO-seq count
tables. No spike-in: size factors come from library size (\code{libSize}, using the
\code{umi_counts} column of the stats files) or DESeq2 defaults. Outputs a DESeq2
object, a fold-change table, and MA plots. Wraps \code{inst/Rscript/deseq2_proseq_analysis.R}.
Submit with \code{\link[=fn_submit]{fn_submit()}}.
}
