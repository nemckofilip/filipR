% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_bowtie1_map.R
\name{fn_bowtie1_map}
\alias{fn_bowtie1_map}
\title{Map Reads with Bowtie1 (PRO-seq)}
\usage{
fn_bowtie1_map(
  fq1,
  index,
  base.name,
  fq2 = NULL,
  output.dir = "db/bam/",
  alignment.stats.output.dir = "db/alignment_stats/",
  suffix = "",
  max.mismatch = 2,
  max.ins = 500,
  cores = 8
)
}
\arguments{
\item{fq1}{Character. Path(s) to R1 FASTQ file(s). Several files are merged
(comma-separated) during alignment.}

\item{index}{Path to the Bowtie1 index prefix (e.g. references .../bowtie1/hg38).}

\item{base.name}{Base name for output files.}

\item{fq2}{Character. Path(s) to R2 FASTQ for paired-end data. Default NULL (single-end).}

\item{output.dir}{Directory for the output BAM. Default 'db/bam/'.}

\item{alignment.stats.output.dir}{Directory for Bowtie1 alignment stats. Default 'db/alignment_stats/'.}

\item{suffix}{Suffix appended to output file names (e.g. '_hg38'). Default ''.}

\item{max.mismatch}{Maximum number of mismatches (\code{-v}). Default 2.}

\item{max.ins}{Maximum insert size for paired-end reads (\code{--maxins}). Default 500.}

\item{cores}{Total number of threads. Default 8.}
}
\value{
data.table with columns: fq1_in, fq2_in, bam, stats, cmd, path.
}
\description{
Builds a shell command to align reads to a reference index using Bowtie1
(\code{-v} mismatch mode, unique mapping only), piping the output to a sorted BAM.
Preserves UMIs in read headers and keeps unaligned reads in the BAM (used by
\code{\link[=fn_umi_collapse_proseq]{fn_umi_collapse_proseq()}} for total-read statistics). Submit with \code{\link[=fn_submit]{fn_submit()}}.
}
