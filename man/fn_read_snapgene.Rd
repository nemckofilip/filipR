% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_read_snapgene.R
\name{fn_read_snapgene}
\alias{fn_read_snapgene}
\title{Read the Sequence from a SnapGene .dna File}
\usage{
fn_read_snapgene(path, as.character = TRUE)
}
\arguments{
\item{path}{Character. Path to a single \code{.dna} file.}

\item{as.character}{Logical. If \code{TRUE} (default) returns an uppercase
character string; if \code{FALSE} returns a \code{Biostrings::DNAString}, which
requires the Biostrings package.}
}
\value{
A character string of the full sequence, or a \code{DNAString} when
\code{as.character = FALSE}.
}
\description{
Extracts the nucleotide sequence from a SnapGene \code{.dna} plasmid map without
any external tool. The format is a chain of segments, each
\verb{[1 byte type][4 byte big-endian length][payload]}; segment type \code{0} holds
the sequence, whose first payload byte is a topology flag rather than a base.
Useful for reading cloning maps directly instead of exporting FASTA by hand.
}
\examples{
\dontrun{
s <- fn_read_snapgene("db/pools_identity/pHL349.dna")
nchar(s)
# locate a restriction site
gregexpr("GGATCC", s)[[1]]
}
}
