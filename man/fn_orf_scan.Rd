% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_orf_scan.R
\name{fn_orf_scan}
\alias{fn_orf_scan}
\title{Scan a genome FASTA for stop-codon-free ORFs in all 6 reading frames}
\usage{
fn_orf_scan(fasta_path, output_dir, base_name, min_codons = 100L)
}
\arguments{
\item{fasta_path}{Path to the genome FASTA file. Must be indexed (\code{.fai}).}

\item{output_dir}{Directory where the output TSV will be written.}

\item{base_name}{Base name for the output file, e.g. \code{"panTro6"} produces
\code{panTro6_orf_scan_results.tsv}.}

\item{min_codons}{Minimum ORF length in codons. Default \code{100L}.}
}
\value{
A one-row \code{data.table} with columns \code{fasta_in}, \code{output}, \code{cmd},
and \code{path} (used by \code{\link[=fn_submit]{fn_submit()}} for skip-if-exists logic).
}
\description{
Generates a shell command to run the ORF scan as a SLURM job via
\code{\link[=fn_submit]{fn_submit()}}. Scans every primary chromosome (\code{chrN}, \code{chrNA/B}, \code{chrX},
\code{chrY}, \code{chrM}) in all 6 reading frames for maximal stretches without stop
codons. No start codon is required. Scaffolds and alts are excluded
automatically. One chromosome is held in memory at a time.
}
\examples{
\dontrun{
cmd <- fn_orf_scan(
  fasta_path = "/references/primate/panTro6/fasta/panTro6.fa",
  output_dir = "db/orf_scan/species_scans",
  base_name  = "panTro6"
)
fn_submit(cmd, job.name = "orf_scan_panTro6", mem = "32G", time = "08:00:00")
}

}
