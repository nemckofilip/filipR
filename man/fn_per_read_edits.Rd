% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_per_read_edits.R
\name{fn_per_read_edits}
\alias{fn_per_read_edits}
\title{Compute per-read editing distribution from a BAM file}
\usage{
fn_per_read_edits(
  bam,
  base.name,
  output.dir = "db/alignment_stats/per_read_edits/",
  min.mq = 20
)
}
\arguments{
\item{bam}{Character. Path to sorted BAM.}

\item{base.name}{Base name for output files.}

\item{output.dir}{Output directory.}

\item{min.mq}{Minimum mapping quality. Default 20 (excludes STAR multi-mappers).}
}
\value{
data.table with columns: bam, output, cmd, path.
}
\description{
Runs \verb{samtools view} piped into a Python parser that uses MD tags and CIGAR
to count all 12 substitution types per read. Assumes an RF-stranded library
(R1 antisense, R2 sense); mismatches are converted to the mRNA/sense-strand
coordinate frame using the flip condition: is_r1 != is_reverse.
}
