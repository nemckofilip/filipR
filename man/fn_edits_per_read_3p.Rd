% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_edits_per_read_3p.R
\name{fn_edits_per_read_3p}
\alias{fn_edits_per_read_3p}
\title{Per-read edit-count distribution split by 3'-proximity}
\usage{
fn_edits_per_read_3p(
  edit.calls.annot,
  base.name,
  output.dir = "db/alignment_stats/edits_per_read_3p/",
  dist.thr = 500
)
}
\arguments{
\item{edit.calls.annot}{Character. Path to an annotated edit-calls RDS
(output of \code{\link[=fn_annotate_edit_calls]{fn_annotate_edit_calls()}}).}

\item{base.name}{Base name for output files.}

\item{output.dir}{Output directory. Default 'db/alignment_stats/edits_per_read_3p/'.}

\item{dist.thr}{Integer. 3'-proximal window (nt): a read is "proximal" if its
closest target base to the 3' end is within this distance. Default 500.}
}
\value{
data.table with columns: output, cmd, path. Output TSV columns:
region ("all" | "prox"), n_edited, n_reads.
}
\description{
Builds a shell command that, from an annotated edit-calls RDS,
tallies the per-read on-target edit-count distribution for all genic reads
and for reads overlapping the 3'-proximal window (closest target base within
\code{dist.thr} nt of the 3' end). Used to ask whether restricting to
3'-proximal reads shifts the edits-per-read survival. Heavy lifting runs in
\code{inst/Rscript/edits_per_read_3p.R}. Submit with \code{\link[=fn_submit]{fn_submit()}}.
}
