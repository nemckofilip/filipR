% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_edited_sites.R
\name{fn_edited_sites}
\alias{fn_edited_sites}
\title{Extract per-position edited sites from a BAM file}
\usage{
fn_edited_sites(
  bam,
  base.name,
  output.dir = "db/edited_sites/",
  fasta,
  edit.types = c("A>G", "C>T"),
  stranded = TRUE,
  min.bq = 20,
  min.mq = 20,
  max.depth = 1e+06
)
}
\arguments{
\item{bam}{Character. Path to sorted, indexed BAM.}

\item{base.name}{Base name for output files.}

\item{output.dir}{Output directory. Default \code{"db/edited_sites/"}.}

\item{fasta}{Path to reference FASTA (must have a .fai index).}

\item{edit.types}{Character vector of edit types to extract,
e.g. \code{c("A>G", "C>T")}. Default \code{c("A>G", "C>T")}.}

\item{stranded}{Logical. Forward-stranded PE (e.g. CORALL). Default TRUE.}

\item{min.bq}{Minimum base quality. Default 20.}

\item{min.mq}{Minimum mapping quality. Default 20.}

\item{max.depth}{Maximum per-position depth cap. Default 1000000.}
}
\value{
data.table with columns: bam, output, cmd, path.
}
\description{
Runs \code{samtools mpileup} on R1 and R2 reads in parallel, parses
mismatches for the requested edit types, and combines the two passes into
a single BED with columns: chrom, start (0-based), end, name (ref>alt),
alt_count, strand, ref_count.
}
\details{
Only positions where \code{alt_count > 0} are written. Strand and ref/alt
are reported at the transcript level (complement applied to reverse-strand
positions).
}
