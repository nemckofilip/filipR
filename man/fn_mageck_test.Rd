% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_mageck_test.R
\name{fn_mageck_test}
\alias{fn_mageck_test}
\title{Test sgRNA/Gene Enrichment Using MAGeCK test}
\usage{
fn_mageck_test(
  count.table,
  treatment,
  control,
  base.name,
  control.sgrna = NULL,
  paired = FALSE,
  norm.method = "control",
  output.dir = "db/counts/"
)
}
\arguments{
\item{count.table}{Path to a MAGeCK count table (\code{.count.txt}).}

\item{treatment}{Character vector of treatment sample labels (must match the
count-table header).}

\item{control}{Character vector of control sample labels.}

\item{base.name}{Output prefix name (MAGeCK \code{-n}).}

\item{control.sgrna}{Optional path to a control sgRNA ID list for the null
distribution (\code{--control-sgrna}). Default NULL.}

\item{paired}{Logical. Paired comparison (treatment/control matched by order,
e.g. rep1 vs rep1)? Default FALSE.}

\item{norm.method}{Normalisation: "none", "median", "total", or "control".
Default "control" (normalise by the control sgRNAs).}

\item{output.dir}{Output directory. Default "db/counts/".}
}
\value{
data.table with columns: gene_summary, sgrna_summary, cmd, path.
}
\description{
Builds a shell command for \verb{mageck test}, comparing treatment vs control
samples from a MAGeCK count table and producing gene- and sgRNA-level
enrichment/depletion statistics (RRA). Submit with \code{\link[=fn_submit]{fn_submit()}}.
}
