% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_edit_calls.R
\name{fn_edit_calls}
\alias{fn_edit_calls}
\title{Extract per-observation edit calls from a BAM file}
\usage{
fn_edit_calls(
  bam,
  base.name,
  output.dir = "db/alignment_stats/edit_calls/",
  fasta,
  edit.type,
  region.bed = NULL,
  min.bq = 20,
  min.mq = 20
)
}
\arguments{
\item{bam}{Character. Path to a sorted BAM.}

\item{base.name}{Base name for the output file.}

\item{output.dir}{Output directory. Default \code{"db/alignment_stats/edit_calls/"}.}

\item{fasta}{Path to reference FASTA (must have a .fai index).}

\item{edit.type}{Single transcript-level edit, e.g. \code{"A>G"} or \code{"C>T"}.}

\item{region.bed}{Optional BED restricting to reads overlapping those
regions (e.g. functional gene bodies). Default \code{NULL} = whole BAM.}

\item{min.bq}{Minimum base quality. Default 20.}

\item{min.mq}{Minimum mapping quality. Default 20.}
}
\value{
A one-row \code{data.table} with columns: \code{bam}, \code{output}, \code{cmd}, \code{path}.
}
\description{
Walks every read once (via \code{inst/python/extract_edit_calls.py}, pysam) and
emits one row per reference target base it covers, keeping read identity so
per-read edit counts and per-site editing both derive from the same table:

\code{read_id}, \code{chrom}, \code{pos} (0-based), \code{strand}, \code{is_edited} (0/1).

\code{read_id} is an integer per fragment (mates share it), so you can filter
reads by edit count (e.g. keep reads with >= 5 edits) and still recover
exactly where those edits fall — the two views are linked. The target base
is set by \code{edit.type} at the transcript level (A>G -> genomic A/G on +
strand, T/C on -). Reference bases come from \code{fasta} (BAM sequences are
original, not 3N-converted). Submit with \code{\link[=fn_submit]{fn_submit()}}.
}
