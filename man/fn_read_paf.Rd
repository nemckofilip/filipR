% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_read_paf.R
\name{fn_read_paf}
\alias{fn_read_paf}
\title{Read a minimap2 PAF File}
\usage{
fn_read_paf(path, select = NULL, dedup = TRUE)
}
\arguments{
\item{path}{Character. Path to a \code{.paf} file.}

\item{select}{Character or \code{NULL}. Subset of columns to keep, e.g.
\code{c("qname", "tname", "nmatch")}. \code{NULL} (default) keeps all 12.}

\item{dedup}{Logical. Drop duplicate query-target pairs, which minimap2
emits when a query aligns to the same target in several blocks.
Default: \code{TRUE}.}
}
\value{
A \code{data.table} with columns \code{qname}, \code{qlen}, \code{qstart}, \code{qend},
\code{strand}, \code{tname}, \code{tlen}, \code{tstart}, \code{tend}, \code{nmatch}, \code{alen}, \code{mapq}
(or the subset given by \code{select}).
}
\description{
Reads the 12 mandatory PAF columns produced by \code{\link[=fn_minimap2_map]{fn_minimap2_map()}} into a
\code{data.table}, with the column names spelled out so callers do not have to.
Trailing key:type:value tags are ignored. Returns a correctly typed empty
table for an empty file, so callers can skip a \code{nrow()} guard.
}
\examples{
\dontrun{
p <- fn_read_paf("db/paf/ppLS1_mapont.paf")

# best target per query, with the runner-up as an ambiguity margin
p <- fn_read_paf("db/paf/ppLS1_mapont.paf",
                 select = c("qname", "tname", "nmatch"))
p[order(qname, -nmatch),
  .(hit = tname[1], margin = if (.N > 1) nmatch[2] / nmatch[1] else 0),
  by = qname]
}
}
