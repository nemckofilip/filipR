% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_count_proseq_reads.R
\name{fn_count_proseq_reads}
\alias{fn_count_proseq_reads}
\title{Count PRO-seq Reads Over Features}
\usage{
fn_count_proseq_reads(
  umi.count.file,
  annotation.file,
  blacklist.file = NULL,
  feature = NULL,
  output.prefix = NULL,
  count.tables.output.dir = "db/count_tables/"
)
}
\arguments{
\item{umi.count.file}{Path to the UMI counts file from \code{\link[=fn_umi_collapse_proseq]{fn_umi_collapse_proseq()}} (single .txt).}

\item{annotation.file}{Path to a \code{.rds} annotation (a data.table with columns
seqnames, start, end, strand, cluster.id). One row per feature (e.g. per gene).}

\item{blacklist.file}{Optional \code{.rds} of blacklisted regions to subtract. Default NULL.}

\item{feature}{Feature label used in the output name. Default NULL (derived from annotation file name).}

\item{output.prefix}{Output prefix. Default NULL (derived from the UMI counts file name).}

\item{count.tables.output.dir}{Directory for the counts table. Default 'db/count_tables/'.}
}
\value{
data.table with columns: umi_count_file, count_table, cmd, path.
}
\description{
Builds a shell command to count PRO-seq UMI reads over annotated features
(strand-specific), optionally subtracting reads in blacklisted regions. Wraps
\code{inst/Rscript/count_proseq_reads.R}. Submit with \code{\link[=fn_submit]{fn_submit()}}.
}
