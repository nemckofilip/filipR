% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_hisat3n_map.R
\name{fn_hisat3n_map}
\alias{fn_hisat3n_map}
\title{Map Reads with HISAT-3N (Conversion-Aware Splice-Aware Alignment)}
\usage{
fn_hisat3n_map(
  fq1,
  fq2 = NULL,
  index,
  base.name,
  base.change,
  output.dir = "db/bam/",
  save.unmapped = TRUE,
  cores = 8,
  mapq = NULL,
  multimapper.mode = "best",
  rna.strandness = NULL,
  alignment.stats.output.dir = "db/alignment_stats/"
)
}
\arguments{
\item{fq1}{Character. Path to R1 FASTQ file (gzip supported).}

\item{fq2}{Character or \code{NULL}. Path to R2 FASTQ for paired-end;
\code{NULL} for single-end.}

\item{index}{Character. Path prefix of the HISAT-3N index built with the
same \code{base.change}.}

\item{base.name}{Character. Sample base name used for output file naming.}

\item{base.change}{Character. Conversion passed to \code{--base-change},
e.g. \code{"A,G"} for TadA/A-to-I samples or \code{"C,T"} for
APOBEC/C-to-U samples.}

\item{output.dir}{Character. Directory for output BAM and unmapped FASTQ.
Default: \code{"db/bam/"}.}

\item{save.unmapped}{Logical. If \code{TRUE}, writes unmapped reads as
gzipped FASTQ. Default: \code{TRUE}.}

\item{cores}{Integer. Total threads for HISAT-3N and samtools sort.
Default: \code{8}.}

\item{mapq}{Integer or \code{NULL}. Minimum MAPQ for post-alignment
samtools view filter. Default: \code{NULL}.}

\item{multimapper.mode}{Character. How to handle multi-mappers:
\code{"best"} (one alignment per read, \code{-k 1}),
\code{"all"} (up to HISAT-3N default of 5 alignments per read),
\code{"unique"} (only uniquely mapped reads; forces \code{mapq = 60}).
Default: \code{"best"}.}

\item{rna.strandness}{Character or \code{NULL}. Passed to
\code{--rna-strandness}: \code{"RF"}, \code{"FR"}, or \code{NULL} for
unstranded. Default: \code{NULL}.}

\item{alignment.stats.output.dir}{Character. Directory for alignment summary
log. Default: \code{"db/alignment_stats/"}.}
}
\value{
A \code{data.table} with columns: \code{fq1_in}, \code{fq2_in},
\code{bam}, \code{stats}, \code{fq1_unmapped}, \code{fq2_unmapped},
\code{cmd}, \code{path}.
}
\description{
Maps reads to a reference genome using HISAT-3N, a conversion-aware
extension of HISAT2. Designed for RNA editing experiments where the edited
base would cause excessive mismatches in a standard aligner (e.g. A-to-I by
TadA variants, C-to-U by APOBEC deaminases). Edited bases in the read are
treated as matches rather than mismatches, recovering reads that would
otherwise be unmapped. Produces a sorted, indexed BAM file. HISAT-3N adds
the \code{Yf:Z:} tag to each alignment recording the number of detected
conversions.
}
