% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_mismatch_stats.R
\name{fn_mismatch_stats}
\alias{fn_mismatch_stats}
\title{Count base substitutions from a BAM file, split by human vs ERCC}
\usage{
fn_mismatch_stats(
  bam,
  base.name,
  output.dir = "db/alignment_stats/mismatch_stats/",
  rf.stranded = FALSE
)
}
\arguments{
\item{bam}{Path to input BAM file (must be indexed).}

\item{base.name}{Base name for output files.}

\item{output.dir}{Output directory.
Default: \code{"db/alignment_stats/mismatch_stats/"}.}

\item{rf.stranded}{Logical; use mRNA-perspective counting for RF
stranded PE libraries (e.g. Lexogen CORALL). Default \code{FALSE}.}
}
\value{
A \code{data.table} with columns \code{bam}, \code{path} (human mismatch
output, used for existence check by \code{\link[=fn_submit]{fn_submit()}}), \code{path_human},
\code{path_ercc}, \code{path_human_pos}, \code{path_ercc_pos}, \code{path_human_base},
\code{path_ercc_base}, \code{path_human_base_pos}, \code{path_ercc_base_pos},
\code{cmd}.
}
\description{
Generates a command that streams all mapped reads from a BAM, parses
MD tags via a bundled Python script, and writes per-substitution-type
counts to two text files (human chromosomes and ERCC spike-ins).
Also writes positional count files and total-base count files so that
per-position mismatch rates can be computed downstream (rate =
mismatch_pos / base_pos).

With \code{rf.stranded = TRUE} (e.g. Lexogen CORALL), substitutions and
base counts are reported from the mRNA perspective: reads from
minus-strand genes are complement-flipped (flip when R1 XOR
reverse-mapped), collapsing e.g. T>C into A>G so that each editing
type appears as a single substitution class regardless of gene strand.
Positions use R1/R2 identity for the 5-prime-of-mRNA flip.
}
\examples{
\dontrun{
cmd <- fn_mismatch_stats("sample.bam", base.name = "sample1",
                         rf.stranded = TRUE)
fn_submit(cmd, job.name = "mismatch_sample1", mem = "8G", cores = 2)
}
}
