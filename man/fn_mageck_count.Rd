% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_mageck_count.R
\name{fn_mageck_count}
\alias{fn_mageck_count}
\title{Count sgRNAs Using MAGeCK count}
\usage{
fn_mageck_count(
  fastqs,
  sample.names,
  library.file,
  base.name,
  control.sgrna = NULL,
  trim5 = "23,24",
  output.dir = "db/counts/"
)
}
\arguments{
\item{fastqs}{A list of character vectors, one element per sample; each
vector holds that sample's FASTQ paths (e.g. the 4 lanes). Lanes are
comma-joined for MAGeCK (technical replicates of one sample).}

\item{sample.names}{Character vector of sample labels, same length and order
as \code{fastqs}. Used for \code{--sample-label} and the count-matrix columns.}

\item{library.file}{Path to the MAGeCK library CSV (\verb{sgRNA,sequence,gene}).}

\item{base.name}{Output prefix name (MAGeCK \code{-n}).}

\item{control.sgrna}{Optional path to a control sgRNA ID list for the null
distribution / normalisation (\code{--control-sgrna}). Default NULL.}

\item{trim5}{5' trimming length(s) passed to \code{--trim-5}; comma-separated
string or "AUTO". Default "23,24".}

\item{output.dir}{Output directory. Default "db/counts/".}
}
\value{
data.table with columns: counts, counts_normalized, summary, cmd, path.
}
\description{
Builds a shell command to count CRISPR sgRNA reads across samples with
\verb{mageck count}, producing a guide x sample count matrix. All samples are
counted in a single job; each sample may span several lane FASTQs, which are
treated as technical replicates (comma-joined). Submit with \code{\link[=fn_submit]{fn_submit()}}.
}
