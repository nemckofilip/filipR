% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_minimap2_map.R
\name{fn_minimap2_map}
\alias{fn_minimap2_map}
\title{Map Reads with minimap2}
\usage{
fn_minimap2_map(
  fq,
  reference,
  base.name,
  output.dir = "db/paf/",
  preset = "map-ont",
  output = "paf",
  cores = 8,
  kmer = NULL,
  window = NULL,
  secondary = FALSE,
  max.secondary = 5,
  min.secondary.ratio = NULL,
  cigar = FALSE,
  extra = NULL
)
}
\arguments{
\item{fq}{Character. Path to the query FASTQ/FASTA (gzip supported).}

\item{reference}{Character. Path to the reference FASTA.}

\item{base.name}{Character. Sample base name used for output file naming.}

\item{output.dir}{Character. Directory for output. Default: \code{"db/paf/"}.}

\item{preset}{Character. minimap2 \code{-x} preset, e.g. \code{"map-ont"},
\code{"map-pb"}, \code{"sr"}, \code{"asm5"}. Default: \code{"map-ont"}.}

\item{output}{Character. \code{"paf"} (default) or \code{"bam"}. \code{"bam"} adds \code{-a},
pipes through \verb{samtools sort} and indexes the result.}

\item{cores}{Integer. Threads passed to \code{-t}. Default: \code{8}.}

\item{kmer}{Integer or \code{NULL}. Passed to \code{-k}. Lower values (e.g. \code{11}) help
when queries are short or error-rich. Default: \code{NULL} (preset default).}

\item{window}{Integer or \code{NULL}. Passed to \code{-w}. Default: \code{NULL}.}

\item{secondary}{Logical. Keep secondary alignments. \code{TRUE} is required if
you want a best-vs-second-best ambiguity margin. Default: \code{FALSE}.}

\item{max.secondary}{Integer. Passed to \code{-N} when \code{secondary = TRUE}.
Default: \code{5}.}

\item{min.secondary.ratio}{Numeric or \code{NULL}. Passed to \code{-p}: report
secondaries scoring at least this fraction of the best. Default: \code{NULL}.}

\item{cigar}{Logical. Add \code{-c} to emit base-level CIGAR in PAF. Ignored for
BAM output. Default: \code{FALSE}.}

\item{extra}{Character or \code{NULL}. Extra flags passed verbatim.
Default: \code{NULL}.}
}
\value{
A \code{data.table} with columns: \code{fq_in}, \code{reference}, \code{out}, \code{cmd},
\code{path}.
}
\description{
Builds a shell command to align reads with minimap2 and returns it without
executing. Suited to long noisy reads (Nanopore, PacBio) but also handles
short queries against a small reference via the \code{preset} and \code{kmer}/\code{window}
arguments. Emits PAF by default, which is what most assignment work needs;
set \code{output = "bam"} for a sorted, indexed BAM.

Submit with \code{\link[=fn_submit]{fn_submit()}}, or for a small job run it directly with
\code{system(cmd$cmd)} — like every other wrapper here it only builds the string.
}
\examples{
\dontrun{
# Nanopore reads to a plasmid reference, PAF out, run locally
cmd <- fn_minimap2_map(fq = "reads.fastq.gz", reference = "refs.fa",
                       base.name = "ppLS1", output.dir = "db/paf/")
system(cmd$cmd)

# short error-rich inserts, keep secondaries for an ambiguity margin
cmd <- fn_minimap2_map(fq = "inserts.fasta", reference = "catalog.fa",
                       base.name = "ppLS1", kmer = 11, window = 5,
                       secondary = TRUE, max.secondary = 20,
                       min.secondary.ratio = 0.4)
fn_submit(cmd, job.name = "mm2", logs = "db/logs/minimap2/")
}
}
