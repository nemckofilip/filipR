% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_countSubread.R
\name{fn_featureCounts}
\alias{fn_featureCounts}
\title{Count RNA-Seq Reads Using featureCounts (Subread)}
\usage{
fn_featureCounts(
  bam,
  base.name,
  annotation.file,
  paired = FALSE,
  strandSpecific = 1,
  allowMultiOverlap = FALSE,
  countMultiMapping = TRUE,
  output.dir = "db/counts/",
  cores = 4
)
}
\arguments{
\item{bam}{Character string. Path to the input BAM file. Only one file allowed.}

\item{base.name}{Base name for output files.}

\item{annotation.file}{Path to a GTF annotation file.}

\item{paired}{Logical. TRUE for paired-end, FALSE for single-end. Default FALSE.}

\item{strandSpecific}{Strand specificity: 0 = unstranded, 1 = forward, 2 = reverse. Default 1.}

\item{allowMultiOverlap}{Logical. Assign reads to all overlapping features? Default FALSE.}

\item{countMultiMapping}{Logical. Count multi-mapping reads? Default TRUE.}

\item{output.dir}{Directory for counts and stats files. Default "db/counts/".}

\item{cores}{Number of threads. Default 4.}
}
\value{
A data.table with columns: bam_in, counts, stats, cmd, job.name, path.
}
\description{
Counts reads from a BAM file using the featureCounts command-line tool.
Outputs a clean counts file (gene_id, gene_name, count) and a statistics file.
Supports single-end and paired-end data, stranded libraries.
}
