% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_gini.R
\name{fn_gini}
\alias{fn_gini}
\title{Gini Coefficient of a Count Distribution}
\usage{
fn_gini(x)
}
\arguments{
\item{x}{Numeric. Counts, one per feature. Zeros are meaningful and are kept;
\code{NA} values are removed.}
}
\value{
A single numeric between 0 and 1, or \code{NA_real_} if all counts are
zero.
}
\description{
Evenness of a count vector: \code{0} when every element is identical, approaching
\code{1} as the counts concentrate into a few elements. Standard summary for
library representation (reads or cells per guide, per construct, per clone).

Note that sampling noise alone puts the Gini well above \code{0} at low coverage,
so a raw value is not interpretable on its own. Compare it against the value
a perfectly even library of the same size and depth would give, which
\code{\link[=fn_gini_poisson]{fn_gini_poisson()}} simulates.
}
\examples{
fn_gini(rep(10, 100))          # 0, perfectly even
fn_gini(c(rep(0, 99), 1000))   # near 1, all reads on one feature

\dontrun{
# a Gini is only interpretable against its sampling floor
counts <- rpois(1000, 4)
fn_gini(counts)
fn_gini_poisson(n = 1000, lambda = 4)
}
}
