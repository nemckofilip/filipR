% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_trimGalore.R
\name{fn_trimGalore}
\alias{fn_trimGalore}
\title{Trim FASTQ Reads with Trim Galore}
\usage{
fn_trimGalore(
  fq1,
  fq2 = NULL,
  base.name,
  output.dir = "db/fq/",
  clip.5p.r1 = NULL,
  clip.5p.r2 = NULL,
  clip.3p.r1 = NULL,
  clip.3p.r2 = NULL,
  adapter.r1 = NULL,
  adapter.r2 = NULL,
  quality = 20,
  min.length = 20,
  poly.a = FALSE,
  cores = 4
)
}
\arguments{
\item{fq1}{Character string. Path to R1 FASTQ file.}

\item{fq2}{Character string. Path to R2 FASTQ file. Default NULL (single-end).}

\item{base.name}{Base name for output files.}

\item{output.dir}{Directory for output FASTQ files. Default "db/fq/".}

\item{clip.5p.r1}{Hard trim N bases from 5' end of R1. Default NULL.}

\item{clip.5p.r2}{Hard trim N bases from 5' end of R2. Default NULL.}

\item{clip.3p.r1}{Hard trim N bases from 3' end of R1. Default NULL.}

\item{clip.3p.r2}{Hard trim N bases from 3' end of R2. Default NULL.}

\item{adapter.r1}{Adapter sequence for R1. Default NULL (auto-detect).}

\item{adapter.r2}{Adapter sequence for R2. Default NULL (auto-detect).}

\item{quality}{Quality cutoff for trimming. Default 20.}

\item{min.length}{Minimum read length to keep after trimming. Default 20.}

\item{poly.a}{Logical. Trim poly-A at 3' of R1 and poly-T at 3' of R2 (PE mode). Passes
\code{--polyA} to Trim Galore. Applies unconditionally to all reads. Default FALSE.}

\item{cores}{Number of threads. Default 4.}
}
\value{
A \code{data.table} with columns: fq1_in, fq2_in, fq1_out, fq2_out, cmd.
}
\description{
General-purpose wrapper for Trim Galore. Handles single-end and paired-end reads.
Supports adapter trimming (auto-detect or custom), hard clipping, and quality trimming.
Keeps read pairs synchronized when filtering.
}
\examples{
# Single-end with default adapter detection
cmd <- fn_trimGalore(fq1 = "sample_R1.fq.gz", base.name = "sample1")

# Paired-end with default adapter detection
cmd <- fn_trimGalore(fq1 = "sample_R1.fq.gz", fq2 = "sample_R2.fq.gz", base.name = "sample1")

# Paired-end with 20bp hard trim from 5' of R2 (e.g., RT primer removal)
cmd <- fn_trimGalore(fq1 = "sample_R1.fq.gz", fq2 = "sample_R2.fq.gz",
                     base.name = "sample1", clip.5p.r2 = 20)

# CORALL library: 9bp R2 5' clip, 12bp R2 3' clip, poly-A/T trimming
cmd <- fn_trimGalore(fq1 = "sample_R1.fq.gz", fq2 = "sample_R2.fq.gz",
                     base.name = "sample1", clip.5p.r2 = 9, clip.3p.r2 = 12, poly.a = TRUE)

}
