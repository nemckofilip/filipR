% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_edit_motif.R
\name{fn_edit_motif}
\alias{fn_edit_motif}
\title{Tabulate reference sequence context around edited vs non-edited bases}
\usage{
fn_edit_motif(
  edit.calls,
  base.name,
  fasta,
  output.dir = "db/alignment_stats/edit_motif/",
  flank = 4,
  edit.min = 1,
  bg.cap = 2e+06
)
}
\arguments{
\item{edit.calls}{Path to a \verb{*_edit_calls.tsv.gz} from \code{\link[=fn_edit_calls]{fn_edit_calls()}}.}

\item{base.name}{Base name for the output file.}

\item{fasta}{Path to reference FASTA (must have a .fai index).}

\item{output.dir}{Output directory. Default \code{"db/alignment_stats/edit_motif/"}.}

\item{flank}{Number of flanking nt on each side of the target base. Default 4.}

\item{edit.min}{Coverage/support threshold. A site is called edited if at
least this many reads support the edit; the background is coverage-matched -
sites with at least this many reads that are NEVER edited (0). Under-covered
sites and the ambiguous \verb{1..edit.min-1} edited zone are excluded. Raise it
(e.g. 5) to drop single-read false-positive calls. Default 1.}

\item{bg.cap}{Max number of unique non-edited (background) sites to retain;
larger sets are randomly subsampled (per-position frequencies stay
unbiased). All edited sites are always kept. Default 2e6.}
}
\value{
A one-row \code{data.table} with columns: \code{edit_calls}, \code{output}, \code{cmd}, \code{path}.
}
\description{
Builds a shell command that tabulates the reference sequence context
(+/- \code{flank} nt, oriented to the transcript strand) around each target base
in an \code{\link[=fn_edit_calls]{fn_edit_calls()}} table, split into an edited set and a non-edited set.
The non-edited target bases are the matched null - same base, same reads,
same regions - so per-position enrichment (edited / background) isolates the
deaminase's sequence preference rather than the genome's base composition.

A per-unique-site view is produced: one context per \code{chrom+pos+strand}, so a
site counts once no matter how many reads cover it (immune to expression
skew). The center position is always the target base by construction; the
signal lives entirely in the flanks. Reference bases come from \code{fasta} and the
\code{-} strand contexts are reverse-complemented so every context reads 5'->3' on
the transcript. The tabulation runs in \code{inst/Rscript/edit_motif.R}. Submit
with \code{\link[=fn_submit]{fn_submit()}}.
}
