% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_edits_per_read.R
\name{fn_edits_per_read}
\alias{fn_edits_per_read}
\title{Per-read edit-count distribution from an edit_calls table}
\usage{
fn_edits_per_read(
  edit.calls,
  base.name,
  output.dir = "db/alignment_stats/edits_per_read/"
)
}
\arguments{
\item{edit.calls}{Path to a \verb{*_edit_calls.tsv.gz} from \code{\link[=fn_edit_calls]{fn_edit_calls()}}.}

\item{base.name}{Base name for the output file.}

\item{output.dir}{Output directory. Default \code{"db/alignment_stats/edits_per_read/"}.}
}
\value{
A one-row \code{data.table} with columns: \code{edit_calls}, \code{output}, \code{cmd}, \code{path}.
The output RDS holds \code{data.table(n_edited, n_reads)} with the total read
count in attribute \code{total_reads}.
}
\description{
Builds a shell command to summarise, from the output of \code{\link[=fn_edit_calls]{fn_edit_calls()}},
how many on-target edits each read carries — i.e. for every read, the number
of its target bases that are edited — and returns the distribution across
reads. Downstream this gives the fraction of reads with at least N edits
(survival curve) or a per-read edit-count histogram. The count runs in
\code{inst/Rscript/edits_per_read_summary.R}. Submit with \code{\link[=fn_submit]{fn_submit()}}.
}
